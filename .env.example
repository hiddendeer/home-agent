# ===========================================
# Home Agent 环境变量配置模板
# ===========================================
# 请复制此文件为 .env 并填写实际的配置值
# 命令: cp .env.example .env
# ===========================================

# ========== 应用配置 ==========
APP_NAME=Home Backend API
APP_VERSION=1.0.0
DEBUG=False
API_V1_PREFIX=/api/v1

# ========== 服务器配置 ==========
HOST=0.0.0.0
PORT=8002

# ========== 安全配置 ==========
# 请在生产环境中使用强随机密钥
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ========== MySQL 数据库配置 ==========
# 请修改为实际的数据库连接信息
MYSQL_HOST=your-mysql-host
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your-mysql-password
MYSQL_DATABASE=record_info

# ========== Milvus 向量数据库配置 ==========
# 请修改为实际的 Milvus 连接信息
MILVUS_HOST=your-milvus-host
MILVUS_PORT=19530
MILVUS_USER=root
MILVUS_PASSWORD=your-milvus-password

# ========== CORS 配置 ==========
# 允许的前端地址，多个地址用逗号分隔
# 示例: http://localhost:5173,http://your-domain.com,https://your-frontend.com
CORS_ORIGINS=http://localhost:5173,http://your-domain.com

# ========== 日志配置 ==========
# 可选值: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ========== LLM 配置 ==========
# LLM 提供商: zhipuai, openai, etc.
LLM_PROVIDER=zhipuai
# 请替换为实际的 API Key
LLM_API_KEY=your-zhipuai-api-key
LLM_API_BASE=https://open.bigmodel.cn/api/paas/v4
LLM_MODEL=glm-4.7
LLM_TEMPERATURE=1.0
LLM_MAX_TOKENS=10000
LLM_TIMEOUT=60

# ========== Embeddings 配置 ==========
EMBEDDING_PROVIDER=zhipuai
# 请替换为实际的 Embedding API Key
EMBEDDING_API_KEY=your-embedding-api-key
EMBEDDING_API_BASE=https://open.bigmodel.cn/api/paas/v4
EMBEDDING_MODEL=embedding-3
EMBEDDING_DIMENSIONS=384

# ========== Docker 部署说明 ==========
# 1. 复制此文件: cp .env.example .env
# 2. 编辑 .env 文件，填写实际的配置值
# 3. 运行: docker-compose up -d
# 4. 查看日志: docker-compose logs -f
